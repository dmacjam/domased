<%= form_for(@facebook_place) do |f| %>
  <% if @facebook_place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@facebook_place.errors.count, "error") %> prohibited this facebook_place from being saved:</h2>

      <ul>
      <% @facebook_place.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :place_id %><br>
    <%= f.text_field :place_id %>
  </div>
  <div class="field">
    <%= f.label :checked %><br>
    <%= f.check_box :checked %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
